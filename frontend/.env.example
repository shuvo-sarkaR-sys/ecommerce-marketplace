# URL of the backend API (see ../backend). Used server-side by Server
# Components (lib/data/home.ts), by middleware.ts, and as the destination
# for the /api/* rewrite proxy in next.config.ts.
BACKEND_URL=http://localhost:5000

# Used for absolute URLs in metadata/Open Graph tags
NEXT_PUBLIC_SITE_URL=http://localhost:3000
